services:
  postgres:
    image: "postgres:17.2"
    ports:
      - "5439:5432"
    environment:
      POSTGRES_USER: "test"
      POSTGRES_PASSWORD: "test"
      POSTGRES_DB: "test"
  server:
    build: .
    env_file: .env
  bot:
    build: .
    command: "npm run bot"
    env_file: .env
